apiVersion: unleash.nais.io/v1
kind: ApiToken
metadata:
  name: watson-sak
  namespace: holmes
  labels:
    team: holmes
spec:
    unleashInstance:
      apiVersion: unleash.nais.io/v1
      kind: RemoteUnleash
      name: holmes
    secretName: watson-sak-unleash-api-token
    environment: {{UNLEASH_ENVIRONMENT}}