apiVersion: unleash.nais.io/v1
kind: ApiToken
metadata:
  name: watson-sak-admin
  namespace: holmes
  labels:
    team: holmes
spec:
    unleashInstance:
      apiVersion: unleash.nais.io/v1
      kind: RemoteUnleash
      name: holmes
    secretName: watson-sak-admin-unleash-api-token
    environment: {{UNLEASH_ENVIRONMENT}}